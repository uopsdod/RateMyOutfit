DROP TABLE MEM;

/* 使用者 */
CREATE TABLE MEM (
 memId          VARCHAR2(5) NOT NULL,
 memName        VARCHAR2(20),
 memAccount     VARCHAR2(60) UNIQUE,
 memPwd         VARCHAR2(20),
 memProfile	    BLOB,
 CONSTRAINT MEM_MEMID_PK PRIMARY KEY (memId)
);

CREATE SEQUENCE mem_seq
INCREMENT BY 1
START WITH 10001
NOMAXVALUE
NOCYCLE
NOCACHE;
INSERT INTO MEM VALUES (mem_seq.NEXTVAL,'samTsai','sam','1111','');

SELECT * from MEM;